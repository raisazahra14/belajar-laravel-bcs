@extends('layouts.skydash')
@section('content')
<x-ui.page-header title="Stok Menipis" description="Barang dengan stok kurang dari atau sama dengan {{ \App\Models\Barang::MINIMUM_STOCK }} unit."><x-ui.button :href="route('barang.index')" variant="outline-secondary" icon="ti-arrow-left">Kembali ke Daftar</x-ui.button></x-ui.page-header>
<x-ui.card><div class="table-heading"><div><h2>Barang perlu perhatian</h2><p>{{ number_format($barang->total(), 0, ',', '.') }} data ditemukan</p></div></div><div class="table-responsive"><table class="table inventory-table"><thead><tr><th class="text-center">No.</th><th>Kode</th><th>Nama Barang</th><th>Kategori</th><th class="text-end">Stok</th><th>Lokasi</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($barang as $index => $item)<tr><td class="text-center">{{ $barang->firstItem() + $index }}</td><td><x-ui.badge variant="primary">{{ $item->kode_barang }}</x-ui.badge></td><td><strong>{{ $item->nama_barang }}</strong></td><td><x-ui.badge variant="info">{{ $item->kategori }}</x-ui.badge></td><td class="text-end"><x-ui.badge variant="danger">{{ number_format($item->stok, 0, ',', '.') }} {{ $item->satuan }}</x-ui.badge></td><td>{{ $item->lokasi }}</td><td><div class="table-actions justify-content-end"><x-ui.button href="/barang/{{ $item->id }}" variant="outline-primary" size="sm" icon="ti-eye">Detail</x-ui.button></div></td></tr>@empty<tr><td colspan="7"><x-ui.empty-state icon="ti-check-box" title="Semua stok aman" description="Tidak ada barang dengan stok menipis." /></td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $barang->links() }}</div></x-ui.card>
@endsection
