@extends('layouts.skydash')
@section('content')
<x-ui.page-header title="Update Stok" description="Catat barang masuk atau keluar dari persediaan."><x-ui.button href="/barang/{{ $barang->id }}" variant="light">Kembali</x-ui.button></x-ui.page-header>
@if(session('error'))<x-ui.alert type="danger">{{ session('error') }}</x-ui.alert>@endif
@if($errors->any())<x-ui.alert type="danger">{{ $errors->first() }}</x-ui.alert>@endif
<div class="row"><div class="col-lg-4 grid-margin"><x-ui.card><h4 class="card-title">Barang</h4><p class="mb-1 text-muted">{{ $barang->kode_barang }}</p><h5>{{ $barang->nama_barang }}</h5><hr><p class="mb-1 text-muted">Stok saat ini</p><h3 class="text-primary">{{ $barang->stok }} {{ $barang->satuan }}</h3></x-ui.card></div><div class="col-lg-8 grid-margin"><x-ui.card><h4 class="form-section-title">Transaksi Stok</h4>@if(request('prediction_id'))<x-ui.alert type="info">Rekomendasi prediksi disalin ke form. Periksa dan ubah jumlah bila perlu; stok baru berubah setelah disimpan.</x-ui.alert>@endif<form action="/barang/{{ $barang->id }}/stok" method="POST">@csrf<div class="form-group"><label for="jenis">Jenis Transaksi</label><select name="jenis" id="jenis" class="form-select" required><option value="">Pilih Transaksi</option><option value="masuk" @selected(old('jenis', request('jenis')) === 'masuk')>Barang Masuk</option><option value="keluar" @selected(old('jenis', request('jenis')) === 'keluar')>Barang Keluar</option></select></div><div class="form-group"><label for="jumlah">Jumlah</label><input type="number" name="jumlah" id="jumlah" class="form-control" min="1" value="{{ old('jumlah', request('jumlah')) }}" required></div><div class="form-group"><label for="keterangan">Keterangan</label><input type="text" name="keterangan" id="keterangan" class="form-control" placeholder="Contoh: Restock atau pemakaian barang" value="{{ old('keterangan', request('prediction_id') ? 'Restock dari prediksi #'.request('prediction_id') : '') }}"></div><div class="d-flex justify-content-end"><x-ui.button href="/barang/{{ $barang->id }}" variant="light" class="me-2">Batal</x-ui.button><x-ui.button type="submit" icon="ti-save">Simpan</x-ui.button></div></form></x-ui.card></div></div>
@endsection
