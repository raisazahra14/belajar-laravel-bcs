@extends('layouts.skydash')
@section('content')
<div class="page-header"><div><h3 class="font-weight-bold">Update Stok</h3><p>Catat barang masuk atau keluar dari persediaan.</p></div><a href="/barang/{{ $barang->id }}" class="btn btn-light">Kembali</a></div>
@if(session('error'))<div class="alert alert-danger">{{ session('error') }}</div>@endif
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="row"><div class="col-lg-4 grid-margin"><div class="card"><div class="card-body"><h4 class="card-title">Barang</h4><p class="mb-1 text-muted">{{ $barang->kode_barang }}</p><h5>{{ $barang->nama_barang }}</h5><hr><p class="mb-1 text-muted">Stok saat ini</p><h3 class="text-primary">{{ $barang->stok }} {{ $barang->satuan }}</h3></div></div></div><div class="col-lg-8 grid-margin"><div class="card"><div class="card-body"><h4 class="form-section-title">Transaksi Stok</h4><form action="/barang/{{ $barang->id }}/stok" method="POST">@csrf<div class="form-group"><label for="jenis">Jenis Transaksi</label><select name="jenis" id="jenis" class="form-select" required><option value="">Pilih Transaksi</option><option value="masuk" @selected(old('jenis') === 'masuk')>Barang Masuk</option><option value="keluar" @selected(old('jenis') === 'keluar')>Barang Keluar</option></select></div><div class="form-group"><label for="jumlah">Jumlah</label><input type="number" name="jumlah" id="jumlah" class="form-control" min="1" value="{{ old('jumlah') }}" required></div><div class="form-group"><label for="keterangan">Keterangan</label><input type="text" name="keterangan" id="keterangan" class="form-control" placeholder="Contoh: Restock atau pemakaian barang" value="{{ old('keterangan') }}"></div><div class="d-flex justify-content-end"><a href="/barang/{{ $barang->id }}" class="btn btn-light me-2">Batal</a><button type="submit" class="btn btn-primary"><i class="ti-save me-1"></i>Simpan</button></div></form></div></div></div></div>
@endsection
